function displayInstrument(instrument) {
    var gitfiddle=instrument.getAttribute("data-instrument-type");
    alert(gitfiddle + " are usually found in " + "Rock Bands.");
}

function displaySymphony(instrument) {
    var boringband=instrument.getAttribute("data-instrument-type");
    alert(boringband +" are usually found in " + "Symphonies.")
}